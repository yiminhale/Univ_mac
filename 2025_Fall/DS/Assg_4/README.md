# Data Structure

## Assg_4

## Requirement

- 소스파일, 헤더파일 제출 --ㅇㅋ
- “소스코드가 여러 개일테니, 학번.zip으로 묶어서 소스과 실행화면을 제출하세요.” --ㅇㅋ
- Stack ADT의 array 버전: Stack_Array.h 에 구현 --완료
- Stack ADT의 linked list 버전은 Stack_Link.h에 구현 --완료
- 이후 위 두개 파일을 #include “”해서 프로그래밍.--ㅇㅋ
- 입력의 역순으로 출력하는 소스코드: Reverse.c --완료
- 입력한 십진수를 이진수로 변환하는 소스코드: Convert.c --완료
- 괄호 포함하는 사칙연산을 수행하는 계산기 소스코드: Calculator.c
    - [ ]  양의 정수 받음
    - [ ]  token으로 분석(빈칸으로 구분이 안 되는 붙은 식도 분석 가능해야 함. C에서 제공하는 token 분석하는 함수도 있음.)
    - [ ]  빈칸 있을 수 있음
    - [ ]  괄호 있을 수 있음
    - [ ]  두자리 이상의 수도 처리 가능해야 함.
    - [ ]  식의 계산 결과는 양수로 가정(음수, 분수 처리 없음)
    - [ ]  Infix → postfix로 변환해야 함.
    - [ ]  수식의 유효성 검사 수행해야 함. e.g. 괄호 열기만 하고 닫지 않았다던지, 이항연산식을 표현한 건지 등.
    - [ ]  이 문제를 위해 Stack_Array.h와 Stack_Link.h를 변형해야 한다면, 변형된 버전은 Stack_Array2.h, Stack_Link2.h 로 이름지어서 활용해야 함.

---